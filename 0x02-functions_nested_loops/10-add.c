#include "main.h"
/* Main header file */

/**
 * add - adds two numbers together
 *
 * @i: holds one value
 * @k: holds another value
 * Return: returns zero
 */
int add(int i, int k)
{
	return (i + k);
}
